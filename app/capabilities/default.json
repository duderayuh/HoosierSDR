{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the main window to use the core API (invoke + events).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
