# app/ — Tauri v2 desktop shell

Lands in Phase 3 (see `docs/ARCHITECTURE.md` §8). One Rust core (`hs-core`),
web frontend with WebGL/WebGPU waterfall and constellation rendering.

Prototype the spectrum view **first** — it's the identified Tauri risk.
