/target
target/
app/gen/
Cargo.lock
*.iq
*.cf32
*.cs16
*.wav
corpus/
node_modules/
/dist/
!app/dist/
.DS_Store
!crates/hs-vocoder/vendor/

# Downloaded RadioReference data stays on the user's machine — the database is
# licensed and no table data belongs in this repository.
talkgroups.csv
*.talkgroups.csv
rr-dump/

# RadioReference app key for local builds — never commit (see app/build.rs)
app/.rr_app_key

# capture artifacts (large, transient)
/captures/
*.cu8
