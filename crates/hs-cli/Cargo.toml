[package]
name = "hs-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "hoosier-sdr"
path = "src/main.rs"

[features]
# Live audio playback via cpal. Off by default so the CLI builds and runs
# headless (WAV output) with no audio backend present.
default = []
audio = ["dep:cpal"]
# Live RTL-SDR capture (pulls Seify + libusb). Off by default.
rtlsdr = ["hs-source/rtlsdr"]
# Live RTL-SDR capture via SoapySDR (librtlsdr) — supports the E4000 (Smartee
# XTR) and other non-R820T2 tuners the pure-Rust driver cannot. Off by default.
soapy = ["hs-source/soapy"]
# Live Airspy capture over libairspy (`brew install airspy`). Off by default.
airspy = ["hs-source/airspy"]
# Download system/site/talkgroup data from RadioReference. Off by default:
# it pulls an HTTPS client and needs an app key plus a premium login.
radioreference = ["hs-core/radioreference"]

[dependencies]
hs-core = { path = "../hs-core" }
hs-p25 = { path = "../hs-p25" }
hs-dsp = { path = "../hs-dsp" }
hs-decoders = { path = "../hs-decoders" }
hs-source = { path = "../hs-source" }
cpal = { version = "0.15", optional = true }
